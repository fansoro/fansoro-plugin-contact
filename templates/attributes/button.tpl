name="{$key}"
{if $button.type}type="{$button.type}"{/if}
{if $button.name}name="{$button.name}"{/if}
{if $button.value}value="{$button.value}"{/if}
{if $button.class}class="{$button.class}"{/if}
{if $button.id}id="{$button.id}"{/if}
